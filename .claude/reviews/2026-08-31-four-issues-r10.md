# レビュー four-issues ラウンド10

- 日付: 2026-09-02
- 範囲: `git diff main...HEAD`
- 対象コミット: 開始時 `73ca7ad`（ラウンド9の修正 `9b8859a` 〜 `a326875` が対象）
- 走らせた reviewer: rust / spec / robustness / comment / architecture
- 重複を潰した結果: **BLOCK 3 / HIGH 2 / MEDIUM 9**
- **13件を修正、2件を記録に回した。** 1所見1コミット（`efbc34b` 〜 `0b0f55c`）

**ラウンド9が持ち込んだ退行が5件**（R10-01 の一部 / R10-02 / R10-05 / R10-06 / R10-09）。
BLOCK の1件は**5本中4本**が独立に指摘した。

## ラウンド9の検証（robustness の実測）

- コーパス609件への1箇所注入 **17綴り × 29,834通り × 4リビジョン**（約203万パース）。
  **`f7d2648 → HEAD` の `SAME→ERR` は全綴りで0件、「`Ok` なのに元より短い」も0件。**
  利得側は `1図以下` の行末注入で `ERR→SAME` 79件など
- 200,000反復の fuzz（3パーサ + 成功分を3形式へ書き戻し）で **panic 0件**
- 速度: コーパス609件 143.6ms（main）→ 149.0ms（HEAD）= **+3.7%**。線形
- 変異を2つ当てて、ラウンド9の修正がテストで守られていることを確認
  （`a_number_ends_there` から `LINE_ENDS` を落とすと17件、`many0(branch_header)` を消すと1件が落ちる）

## 所見

### R10-01 [BLOCK] KI2 の「本譜の前の `変化：`」の読み飛ばしが、位置と字下げの両方で穴を残している

- 場所: `src/parser/ki2.rs:437` `many0(branch_header)`（R9-02）と `:262-264` `branch_header`
  （`branch_header_ply(line)` を余白を落とさずに呼ぶ）、`:389-421` `attach_branch` の裸の `return`
- 指摘: architecture（BLOCK）/ comment（BLOCK）/ spec（BLOCK）/ robustness（BLOCK）— **4本が独立に**
- 2つの穴が同じ結果（`Ok` 0手）を生む:
  1. **字下げ**: `branch_header` だけが余白を通らない。`padding` の doc は
     「**Every line parser starts with this**」と書いており、この関数で偽
  2. **位置**: `many0(branch_header)` はヘッダブロックの直後1回しか効かない。
     間に散文や `#` を挟むと届かない
- 実測（`先手：A` / `<between>` / `変化{colon}2手` / `▲７六歩 △３四歩 ▲２六歩`。
  同内容の `.kif` は全て `Ok`）:

  | `<between>` | main | f7d2648 | **HEAD** |
  | --- | --- | --- | --- |
  | 無し | Err | Ok 0手 | `Ok` 3手 ✅ |
  | `*まえがき` / `先手番` | Err / Ok 0手 | Ok 0手 | `Ok` 3手 ✅ |
  | **`解説`（散文1行）** | Ok 0手 | Ok 0手 | **`Ok` 0手** |
  | **`# note` + `変化:2手`** | **Err** | Ok 0手 | **`Ok` 0手** |
  | **`変化:2手` を字下げ**（` ` / `　` / `\t`） | **Err** | Ok 0手 | **`Ok` 0手** |

- **`main` の「うるさい拒否」が「静かな全損」に変わっている**綴りが2種ある
- **私の R9-02 のテストは片方の腕が新しいコードを1行も通っていなかった**（spec が指摘）。
  全角コロンで直後に置いた `変化：2手` は `information_line_keyvalue` が
  `header["変化"]="2手"` として先に食う。**緑を守られている証拠として読めない**
- 仕様: D1、R-JKF-004、D18、R-REQ-004
- 直し方: 本譜を読み始めるまでの読み飛ばしを `move_run` の集合と1本にし、
  `branch_header_ply` 自身が余白を消費する（呼び手は全て既に trim しているので受理集合は変わらない）。
  併せて `attach_branch` が置き場を見つけられなかったことを戻り値で表す
- **結果**: 直した（`efbc34b`）: `branch_header_ply` 自身が余白を消費し、本譜の前の読み飛ばしを「コメントを溜めながら回る1つのループ」にした。8種の挟み物 × 4字下げ × 2コロン = **48通り**が全部3手になることと、コメントが原文の順で残ることをテストで固定

### R10-02 [BLOCK] `変化：3手目` のような接尾辞付きが分岐ヘッダでなくなり、分岐が本譜に化けて**勝者が入れ替わる**

- 場所: `src/parser/kakinoki.rs:214-230` `opens_a_branch_header` / `a_number_ends_there`（R9-05 が新設）
- 指摘: robustness
- **R9-05 が持ち込んだ。** `変化：2手を参照` を注記として通すために「数の直後」を絞ったが、
  **絞った側の代償を測っていなかった**
- 実測（`.ki2`、本譜3手 + `変化：3手目` + 分岐2手 + `まで5手で投了`）:

  | | main | f7d2648 | **HEAD** |
  | --- | --- | --- | --- |
  | 結果 | `Ok` 本譜3手 + 分岐1本 | 同 | **`Ok` 本譜5手・分岐0本** |
  | `to_ki2` で書き戻すと | `…\n変化：3手\n…\nまで4手で後手の勝ち` | 同 | **`…▲２六歩 △８四歩 ▲２五歩\nまで5手で先手の勝ち`** |

- **分岐が消えるだけでなく勝者が入れ替わる。** obs-shogi は保存のたびに `to_ki2_owned` を通すので、
  **別の対局・別の勝者になった棋譜が原本を上書きする**（R-REQ-002 / D4）。エラーは1件も出ない
- 同じ形が `変化：3手。` `変化：3手、` `変化：3手：` `変化：3手.` `変化：3手/2` などすべてで起きる
- tsshogi は `/^[ 　]*変化[：:][ 　]*/` のあと数を読むだけで、**後ろに何が付いていても分岐ヘッダ**
- **`main` は4通りとも寛容だった**（実測: 空ブロックの `変化：2手` も KIF / KI2 とも `Ok`）
- 直し方（**判断が要る**。下記「ユーザーに報告すること」）: `変化：<数字>` を接尾辞に関わらず
  分岐ヘッダとして数え、**「行が数字で終わっているか」は空ブロックを報告するかどうかの判定に使う**。
  - `変化：2手` + ブロックあり → 分岐（変わらず）
  - `変化：2手` + ブロック空 → `Err`（D1 の診断を保つ）
  - `変化：2手を参照` + ブロック空 → 散文として読み飛ばす（R9-05 が守りたかったもの）
  - `変化：3手目` + ブロックあり → 分岐（**R10-02 が直る**）
- **結果**: 直した（`efbc34b`）。**ユーザー判断（2026-09-02）**: 「行が数字（と `手`）で終わっているか」は分岐ヘッダかどうかではなく**空ブロックを報告するかどうか**の判定に使う。`変化：3手目` + ブロックあり → 分岐、`変化：2手` + ブロック空 → `Err`（D1）、`変化：2手を参照` + ブロック空 → 読み飛ばす（`main` と同じ）

### R10-03 [BLOCK] 盤面図が1文字でも壊れると、`main` が拒んでいたファイルが `Ok`・盤面なし・平手で返る

- 場所: `src/parser/kakinoki.rs:794-826` `board_row` / `board`（行**末**の余白を許さない）と
  `:460-486` `not_move_line`（盤面の行を散文として読み飛ばせる）
- 指摘: robustness
- **R8-01 が持ち込み、R9-01 は片側（行頭）しか塞いでいない**
- 実測（詰将棋の題材、盤面12行のうち1行だけ壊す）:

  | 壊し方 | main | f7d2648 | **HEAD** |
  | --- | --- | --- | --- |
  | どこか1行の**行末に空白1つ**（48通り） | **`Err`** | `Ok`・盤面なし・平手 | **同左** |
  | 枠線の `-` が1本足りない / 段見出しが半角 / 8マス / `v` が `V` | **`Err`** | 同左 | 同左 |
  | 盤面の途中でファイルが切れている（11通り） | **`Err`** | 同左 | 同左 |
  | 行**頭**の余白 | `Err` | `Ok`・盤面なし | **`Ok`・盤面あり** ✅ |

- 書き戻すと原本が消える: `to_kif` が `手合割：詰将棋\n手数----指手---------消費時間--\n` の2行だけを返す
- 行末の空白はエディタ・Web からの貼り付けで普通に付く。**`is_padding` の doc がその前提を述べている**
- 直し方: **2つ揃えて入れる**（片方だけは今の状態を作る）。
  1. `board` / `board_row` の `line_ending` を `preceded(padding, line_ending)` に
  2. `not_move_line` が行頭 `+` / `|` の行を declines する → 残り入力検査（D1）が報告する。
     **コーパス609件に `^\s*[|+]` の行は0行**
- **結果**: 直した（`942399b`）: `board` / `board_row` の行末も `padding` を通し、`not_move_line` が行頭 `|` `+` の行を declines する。**両方**入れた（片方だけは今の状態を作る）。12行 × 3種の行末余白 = 36通りで board が残ること、枠線・段見出し・マス数・途中切断は `Err` に戻ることをテストで固定

### R10-04 [HIGH] 持駒の値の**前**の余白だけ今も `Err`。盤面図を持つ棋譜がファイルごと落ちる

- 場所: `src/parser/kakinoki.rs:549` `information_value_hand` / `:660` の `fail`
- 指摘: rust
- R8-04 が値の**後ろ**、R9-14 が「値が余白だけ」を通した。**残ったのは「余白 → 値」の順だけ**
- 実測: `なし` / `歩` / 空 / `' '` は `Ok`。**`' なし'` / `' 歩'` / `'　歩'` / `'\t歩'` は `Err ... in Tag:`**
  （`main` も同じで退行ではない）
- tsshogi の `readHand` は `split(/[ 　]/)` して空の断片を飛ばすので**同じファイルが TS 側では開く**
- `Failure` なので盤面図ごとファイルが落ち、エラーはどの語も名指さない
- 直し方: `information_value_hand` の先頭に `padding` を置き、`:660` の `from_error_kind` を
  同じモジュールが持つ `broken_line(line, "this hand line cannot be read")` に替える
- 併せて: 最後の腕のコメント「**Last**, so that it answers only for…」は効いている不変条件ではない。
  **腕を先頭へ移す変異でも171件通る**——守っているのは順序ではなく `peek(line_ending)`
- **結果**: 直した（`942399b`）: 値の前にも `padding` を置き、`fail` を `broken_line` に替えて「this hand line cannot be read」と行を名指しさせた

### R10-05 [HIGH] `opens_a_move_line` が `move_line` より狭く、走りの先頭の指し手行だけが黙って消える

- 場所: `src/parser/kakinoki.rs:309-314` `opens_a_numbered_line`（余白を**要求**する）と
  `src/parser/kif.rs:268` `move_line`（`padding` は0個以上なので要求しない）
- 指摘: robustness
- **R9-04 / R9-08 が `変化：` について構造で成り立たせた「数える集合 = 読める集合」が、
  指し手行では破れている**
- 実測（`変化：` ブロックの1手目だけ空白を落とす）:

  | 分岐の1行目 | main | f7d2648 | **HEAD** |
  | --- | --- | --- | --- |
  | `   2 ８四歩(83)` | `Ok` 分岐2手 | 同 | `Ok` 分岐2手 |
  | **`   2８四歩(83)`** | `Ok` 分岐**2**手 | 同 | **`Ok` 分岐1手（黙って1手消える）** |
  | `   2A８四歩(83)` | **`Err`** | **`Err`** | **`Ok` 分岐1手** |

- GAP-020 の回帰値も動いた: KIF **12→15** / KIF+変化 **36→48** / KI2 6→6
- **同じファイルの中で「これは指し手行か」の答えが位置によって変わる**（2行目以降なら読める）
- 直し方: `opens_a_move_line` を「読み手がこの行を指し手行として読めるか」にする。
  これで R10-14（数字＋空白の注記）も同時に解ける
- **結果**: 直した（`8133451`）: **余白がある場合は従来どおり指し手行**（`   2 パス` を D1 に届けるため。D8）、**余白が無い場合だけ「本当に指し手か」を読む側に聞く**。`   2A８四歩(83)` も `main` と同じ `Ok`（散文）に戻った

### R10-06 [MEDIUM] `is_not("：:\r\n")` が、キーに半角コロンを含むヘッダ行を消す。**自分が書いた KIF を自分で読めない**

- 場所: `src/parser/kakinoki.rs:708`（R9-06）
- 指摘: comment / rust / robustness — 3本
- **R9-06 が持ち込んだ。** `main` はキーの終端を全角 `：` だけで決めていた
- 実測:

  | 入力 | main / f7d2648 | **HEAD** |
  | --- | --- | --- |
  | `備考:あり：なし` | `Ok header["備考:あり"]` | **`Ok header={}`。行ごと消える** |
  | `URL:http://example.com：メモ` | `Ok header[…]` | **`Ok header={}`** |
  | `13:00：開始` | `Ok header["13:00"]` | **`Ok header={}`** |

- 書き手を経由すると `Err`: `{"header":{"a:b":"c"}}` → `to_kif` → `a:b：c` → `parse_kif_str` = **`Err`**。
  **書き手が読み手の拒む KIF を書く**（GAP-028 の KIF 版）。`main` では往復していた
- コーパス609件に該当0行。obs-shogi が入れるキー5つはどれも `:` を含まない
- **GAP-031 は「区切りが半角コロン」の話で、この形（区切りは全角、キーの中に半角）は説明が付かない**
- 直し方: 全角の規則を**先に**試し、落ちたときだけ半角 + 既知キーの規則へ回す
- **結果**: 直した（`8133451`）: 全角の規則を先に試し、落ちたときだけ半角 + 既知キーの規則へ回す。R9-06 の利得（`手合割:詰将棋`）は残り、JKF ファイルは依然 `Err`

### R10-07 [MEDIUM] 既存 doc の直後への挿入が、ラウンド9で**2箇所**新たに入った（6回目・7回目）

- 場所: `src/parser/kakinoki.rs:672-695`（`information_line_keyvalue` の17行が
  `a_key_that_names_its_line` に付いた）、`src/parser.rs:649-686`（テストの説明が1つずれた）
- 指摘: comment（HIGH）/ rust / architecture — 3本
- **R3-17 / R4-04 / R6-13 / R8-08 / R9-09 と同じ故障で、R9-09 を直したのと同じラウンドで2件。
  人の注意では止まっていない**
- `LineShapes::carries_a_line` の doc が「see [`information_line_keyvalue`]」と指す先が無 doc になった
- 機械化: `#![warn(clippy::missing_docs_in_private_items)]` は**いま入れると103件出る**
  （`parser/kakinoki.rs` だけで29件）。ファイル単位で埋めてから `deny` にすれば、
  **この故障だけは確実に落ちる**。`cargo doc -D warnings`（R8-06）は素通しする
- **結果**: 直した（`0b0f55c`）: `information_line_keyvalue` の17行を持ち主へ戻した。**`src/parser.rs` 側はこのコミットでは動かしていない**（ラウンド11 の R11-12 で指摘され、`the_extension_chooses_which_encoding_is_tried_first` の頭へ移した）

### R10-08 [MEDIUM] `LineShapes::opens_a_move_line` の doc が「共有の形ではない」と言うが、4つの実体すべてが同じ関数

- 場所: `src/parser/kakinoki.rs:180-186`（doc）に対して `kif.rs:97` / `ki2.rs:100-103` /
  `kakinoki.rs:1191` / `:1197`
- 指摘: architecture / comment
- doc は「"starts with a digit" is a KIF shape and not a shared one」と言い、
  `ki2::SHAPES` は同じ値に「KIF の指し手行が紛れた `.ki2` は飛ばさない」と**正反対の理由**を書いている
- `LineShapes` の doc 自身が「A rule only one format has does not belong here」と宣言しており、
  **両形式が同じ答えを返す規則を形式ごとのフックにした**のはその逆
- 呼び手3箇所が `|input| not_move_line(SHAPES, input)` というクロージャを書く羽目になり、
  同じモジュールの `information_line_keyvalue(shapes)` とは引数の渡し方も割れた
- **結果**: 直した（`0b0f55c`）: `LineShapes::opens_a_move_line` を落とし、`not_move_line` が `opens_a_numbered_line` を直接呼ぶ。呼び手3箇所のクロージャも消えた

### R10-09 [MEDIUM] キーとコロンの間の余白がキーに焼き付き、`手合割 ：香落ち` が手合割を失う

- 場所: `src/parser/kakinoki.rs:708`
- 指摘: architecture
- R9-01 は「キーに余白が焼き付く」を場所として名指ししたが、入ったのは**行頭側だけ**
- 実測: `手合割 ：香落ち` / `手合割　：香落ち` / `手合割\t：香落ち` は
  **`Err failed to normalize: Invalid move: ８三→８四 at ply 1`**（`main` も同じ）。
  平手でも合法な棋譜なら `header["手合割 "]` に入って平手に落ち、**駒落ちの手番が全反転**。
  D16 の `contains_key` も外れて `手合割` の行が2本並ぶ
- **結果**: 直した（`0b0f55c`）: キーの末尾を trim し、`手合割` / `…の持駒` はキーワードとコロンの間の余白も食う

### R10-10 [MEDIUM] ラウンド9の追加コメント2箇所に「直す前はこうだった」が入った（**5回目**）

- 場所: `src/parser/kakinoki.rs:1477-1482`、`src/parser/ki2.rs:842-846`
- 指摘: comment
- 「Before the readers looked past the indentation, the padding itself satisfied the test」
  「Only the outer skip in `move_run` knew the wider set」
- **R1-09 / R4-05 / R6-14 / R7-10 に続く5回目で、5回とも直した本人の説明文として入っている**
- **結果**: 直した（`0b0f55c`）: 2箇所とも現在の契約だけにした

### R10-11 [MEDIUM] `move_to` が引く `R-NOT` は存在しない ID

- 場所: `src/parser/kakinoki.rs:856-861`
- 指摘: comment
- `grep -rn "R-[A-Z]\+\b[^-]" src/` はこの1行だけを返す。実在するのは **R-NOT-002**
  （「KIF では `同　` が通常だが、全角空白が無い場合もあり得る」）と R-KIF-005
- CLAUDE.md は ID を `R-<領域>-<番号>` と決めており、`R-NOT` では grep で辿れない。
  **R1-10 → R4-03 → R7-09 → R9-12 の流れで、今度はそもそも実在しない ID**
- **結果**: 直した（`0b0f55c`）: `R-NOT-002` に

### R10-12 [MEDIUM] `SIDE_MARKS` の doc が、「refused」と言った直後に「skipping … is the trade D1 takes」と書いている

- 場所: `src/parser/kakinoki.rs:80-84`
- 指摘: comment
- 後半だけを読むと **D1 が読み飛ばしを選んだことになる**。実測では拒む側で、
  引かれているテストも拒むことを固定している
- **R8-07 はまさにこの doc が嘘をついたことの指摘で、直した文がもう一度逆向きにずれている**
- **結果**: 直した（`0b0f55c`）: 「refusing such a line even where it turns out to be prose is the trade D1 takes」に

### R10-13 [MEDIUM] `opens_a_branch_header` が行ごとに `VerboseError` をヒープに積む

- 場所: `src/parser/kakinoki.rs:214-216`（R9-08 の書き換え）
- 指摘: rust
- 同じクレートの `skip_interruptions` が「`VerboseError` の確保がコーパス読み取りの13%だった」ので
  手書きにされている。その隣で同じことをしている
- 実測（release、指し手行3000本すべてに `+` を付けて `ends_here` の第1段を外させたもの）:
  `f7d2648` 4.58ms → **HEAD 5.26ms（+13%）** → `starts_with("変化")` の1行を足すと 4.55ms。
  **コーパス609件では差が測れない**
- **結果**: 直した（`efbc34b`）: `starts_with("変化")` を前に置いた。合成ケースで 5.26ms → 4.55ms

### R10-14 [MEDIUM] 「数字＋空白」で始まる注記は `main` の `Ok` からいまも `Err`。R9-04 の直しが片側

- 場所: `src/parser/kakinoki.rs:309-314` / `:479`
- 指摘: robustness
- R9-04 のテストが固定したのは**数字の直後が余白でない綴り**だけ
- 実測: `　1 図以下、先手優勢` / `　3 手目は疑問` / `　1 序盤の課題` / `　10 分切れ負け` が
  `main` の `Ok` から **`Err`**（ファイル全体）
- **R10-05 を直すと自動的に解ける**
- **結果**: **記録した**（`research/90-gaps.md` GAP-020 の1 に追記）。`　1 序盤の課題` と `   2 パス` は内容から区別できず、前者を通すと後者が黙って消える。**D1 を優先して後者に寄せた**。`main` は「行頭が半角空白かどうか」で偶然分けていただけ

### R10-15 [MEDIUM] `LineShapes` のフィールドを述語ではなくパーサにすれば、この2ラウンドの BLOCK が型で閉じる

- 指摘: robustness（lint 節）
- R10-01 / R10-02 / R10-05 はすべて「数える述語」と「読むパーサ」の対の食い違い。
  フィールドを `fn(&str) -> IResult<...>` にして `opens_a_*` を `parser(head).is_ok()` からしか
  作れないようにすれば型で閉じる。**R9-08 が `変化：` に対して手でやったことを構造にする**
- **範囲が大きいので、このラウンドでは記録のみ**
- **結果**: **記録のみ**。`LineShapes` のフィールドをパーサそのものにする案は範囲が大きい。R10-05 / R10-08 でその方向へ2歩進めた（`opens_a_numbered_line` が `kif::a_move_follows_the_number` を呼び、フィールドが1本減った）

## ユーザーに報告すること（判断が要る）

**R10-02 の直し方**は、ラウンド4で入れた診断を1つ弱める。

- いまは `変化：2手` の下にブロックが無いと `a 変化 block with no moves under it` で
  **ファイル全体を `Err`** にする（D1 の「branch が消えたことを黙らない」）
- **`main` はこれを `Ok` にしていた**（実測）。tsshogi も無視する
- R10-02 を直すには `変化：<数字>` を接尾辞に関わらずヘッダとして数える必要があり、
  そうすると `変化：2手を参照` も「ブロックの無いヘッダ」になる

**採る案**: 「行が数字（と `手`）で終わっているか」を**空ブロックを報告するかどうか**の判定に使う。

| 入力 | 結果 |
| --- | --- |
| `変化：2手` + ブロックあり | 分岐（変わらず） |
| `変化：2手` + ブロック空 | **`Err`**（D1 の診断を保つ） |
| `変化：2手を参照` + ブロック空 | 読み飛ばす（`main` と同じ） |
| `変化：3手目` + ブロックあり | 分岐（**R10-02 が直る**） |

これで `main` の受理集合を保ったまま D1 の診断も残る。**この線引きでよいかを確認したい。**

## 数えた結果（architecture）

| 知識 | R9 | **R10** |
| --- | --- | --- |
| 手番記号 | 2 | **2** |
| 注記の印 | 1 | **1** |
| 行末の集合 | 2 | **3**（増えたのではなく `kakinoki.rs:708` が今まで数えられていなかった。main 由来） |
| 終局語 | 3 | **3** |
| 駒種の表記 | 4 | **4** |
| 「これは `変化：` の行か」 | 1 | **1** |
| **行頭の余白とは何か** | 4 | **1**（`space0` は production から全滅） |
| **コロンとは何か** | 3 | **2** |

公開面は `main` と**完全一致（82項目、diff 0行）**。`Cargo.toml` はバイト同一。
リポジトリ直下の新規ファイル0。`src/` は production 4,906 → 6,165 行（**1.26倍**）、
モジュールの増減なし。

## 見ていない範囲

- `.ki2` / `.csa` / `.jkf` の実コーパスが1件も無い
- CSA 経路はラウンド9の差分に無いため誰も読んでいない
- obs-shogi はソース読みと `node_modules/tsshogi` のみ。ビルドしていない
- 盤面図の**行の中**（桁ずれ、駒種の異体）、深い入れ子の `forks`（GAP-019）、
  `normalize` の冪等性、BOM（GAP-006）

## 修正後の検証

- `bash .claude/verify.sh` — 通る（テスト174件。ラウンド9終了時は171件）
- コーパス609件（`~/Desktop/temp`）の読み書き:
  - **ラウンド5終了時（`771cd0f`）とバイト一致**
  - `main` との差は意図した分だけ: 読み取り 0件 / `to_kif` 0件 /
    `to_ki2` 1件（`bug_mega.kif`）/ `to_csa` 33件（GAP-023）
- 途中で `a_move_follows_the_number` を厳しくしすぎ、`   1 ７六歩(00)` と
  `   2 パス` を黙って読み飛ばす版を書いた。**既存テスト2件が即座に落として捕まえた**

## research/ へ書き戻したもの

- **GAP-020 の1 に追記**: 「数字 → 余白 → 中身」の注記は指し手行として扱われ、
  語が読めなければ拒まれる。`　1 序盤の課題` と `   2 パス` は内容から区別できず、
  **D1 を優先して後者に寄せた**（取引の両側を並べて記録）
- **GAP-031 に追記**: 区切りが全角でキーの中に半角コロンがあるだけの行は消えない
  （ラウンド9で一度そうなっていたのを R10-06 で戻した）

## 次ラウンドの対象

ラウンド10の修正。特に:

- `変化：<数字>` を接尾辞に関わらずヘッダとして数えるようにしたこと（R10-02）の波及。
  `a_branch_header_is_all_the_line_says` が空ブロックの報告だけを決めること
- 本譜の前の読み飛ばしを1つのループにしたこと（R10-01）——読み飛ばしすぎていないか
- `not_move_line` が `|` `+` を declines するようになったこと（R10-03）
- `opens_a_numbered_line` が `kif::a_move_follows_the_number` を呼ぶようになったこと（R10-05）。
  **`parser/kakinoki.rs` が `parser/kif.rs` を参照する向き**が正しいか
- `LineShapes` が2本になったこと（R10-08）
- キーの trim（R10-09）と、全角優先のコロン（R10-06）

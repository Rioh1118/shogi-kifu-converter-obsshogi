# レビュー review-r2 ラウンド3

- 日付: 2026-08-30
- 範囲: `git diff origin/main...HEAD`（ブランチ `fix/review-r2`）
- 対象コミット: レビュー時 `6949900`。修正後 `67028aa`
- 走らせた reviewer: **test のみ**
- 所見 ID は R5-xx

ラウンド1は `2026-08-30-review-r2-r1.md`（R3-xx）、
ラウンド2は `2026-08-30-review-r2-r2.md`（R4-xx）。

**このラウンドは7観点を回していない。** ラウンド2の報告書は「`normalizer.rs` の
曖昧性解消を書き直したので7観点すべてを回し直す」と書いたが、実際に走らせたのは
test だけである。**指摘ゼロのラウンドはまだ1回も出ていない。**

---

## 直したもの

test-reviewer は生存変異17件と BLOCK 3件を挙げた。**変異を実際に当てて落ちることを
確認できた分だけ**を直し、全て `67028aa` に入っている。

| # | 深刻度 | 所見 | 変異で確認 |
| --- | --- | --- | --- |
| R5-01 | **BLOCK** | **`parse_jkf_str` にテストが1件も無い。** GAP-013 はこの入口しか踏めない欠陥で、直したのに入口側の回帰テストが無かった | `from.is_none()` も移動元探索に流す → 落ちる |
| R5-02 | **BLOCK** | 分岐 gate のテスト（`a_branch_past_the_lost_board_is_left_alone`）が**偶数手で分岐していた**ので、gate を外しても通る。停まった局面と手数の偶奇が同じ手番を指すため | gate を外す → 落ちる（奇数手に変更後） |
| R5-03 | **BLOCK** | **読み手と書き手が `suffix_for` を共有した結果、`suffix_for` の誤りは往復テストでは原理的に検出できない。** 文字列を直接固定するテストが無い | — （R-NOT-002/003/004/005 を文字列で固定） |
| R5-04 | HIGH | 終局が手数を取ることが、**途中終局**を含む形で固定されていない | `occupies_a_ply` から `special` を落とす → 落ちる |
| R5-05 | HIGH | `to_usi_owned` の上書きにテストが無い。上書きを外すと `debug_assert!` でプロセスが落ちる入力がある | 上書きを消す → 落ちる（`clippy.toml` の禁止に `#[allow]` が要る唯一の場所） |
| R5-06 | MEDIUM | `.kifu` / `.ki2u` の UTF-8 経路に**テストが1件も無い**。`data/tests/` にその拡張子のフィクスチャが無いため | `.ki2u` を Shift-JIS で読む → 落ちる。`.ki2` を UTF-8 で読む → 落ちる |
| R5-07 | MEDIUM | KIF の「打」必須・「不成」禁止（R-KIF-006）と KI2 のその逆（R-NOT-003 / R-NOT-005）が固定されていない | — （文字列で固定） |
| R5-08 | MEDIUM | 盤外の移動元を KIF / CSA が綴らないことが固定されていない | — |
| R5-09 | MEDIUM | 印刷で使う「行」「生」（R-NOT-006 / R-KI2-005）が KI2 読み手のテスト表に無い | — |

## 直していないもの

| # | 深刻度 | 所見 | 送り先 |
| --- | --- | --- | --- |
| R5-10 | MEDIUM | **`populate_relative()` がパーサの受理した棋譜に `Err` を返す。** 中断後に局面を失った棋譜で `MakeMoveFailed`。`to_ki2` は同じ入力を書けている | **ブランチB**。`research/90-gaps.md` GAP-016 |
| R5-11 | MEDIUM | **KI2 読み手の `occupies_a_ply` フィルタが本譜では効かない。** `out.len()` に変異させてもテスト101件が全て通る | **ブランチB**。`research/90-gaps.md` GAP-017 |
| R5-12 | LOW | 桂馬の曖昧性解消は変異と等価（`by_motion` を通っても `by_file` を通っても同じ答えになる配置しか無い）。テストで殺せない | 記録のみ。等価変異はテストの不足ではない |
| R5-13 | — | `parse_kif_file` の**パース失敗による再デコード**（`Err(ParseError::Kif(_)) if encoding == SHIFT_JIS`）は KIF では到達しない。全角形（`７`『：』）の UTF-8 先頭バイトが `0xEF` で、Shift-JIS にその字が無いため**デコードの時点で** `had_errors` が立ち、手前の再試行が先に効く | 変異が生き残ることを確認済み。消すかどうかはブランチBで判断 |

### R5-10 / R5-11 の設計上の見立て

どちらも**同じ規則が片方にしか無い**形をしている。

- R5-10: 「局面を失ったら以降を触らない」を `normalize_moves` は持ち、
  `populate_relative_moves` は持たない。規則を関数の外に出さない限り、
  木を歩く関数を増やすたびに同じ穴が空く。
  加えて D2 で `to_ki2` が `relative` を読まなくなったので、
  **`populate_relative()` 自体の存在意義**（R-REQ-006）を先に決める必要がある
- R5-11: 書き手のフィルタは本物（JKF は直接コメント専用ノードを持てる）。
  読み手だけが**自分では作れない形**に備えている。
  残すなら「JKF 由来の木を KI2 に書いて読み戻す」経路で効くことを示すテストが要る

## 数字

テスト 96 → **101**。`bash .claude/verify.sh` は green。
コーパス609件の往復忠実度・性能はラウンド2から変更なし
（このラウンドはテストしか触っていない）。

## 見ていない範囲

- **spec / rust / architecture / comment / perf / robustness / oss-hygiene を回していない。**
  ラウンド2で `normalizer.rs` を書き直したままである
- test-reviewer の生存変異17件のうち、**等価変異と判断した分（R5-12）以外にも
  未着手が残っている**。潰した件数は数えていない
- `data/tests/` の期待 JSON が仕様由来か上流実装の出力か（ラウンド2から継続）

## 次にやること

1. **7観点を回し直す。** 指摘ゼロのラウンドが1回出るまで終わらせない
2. R4-16（`try_to_{kif,ki2,csa}_owned` のエラー型）を**タグを打つ前に**決める
3. R5-10 / R5-11 はブランチBへ（GAP-016 / GAP-017）

# レビュー review-r2 ラウンド1

- 日付: 2026-08-30
- 範囲: `git diff origin/main...HEAD`（ブランチ `fix/review-r2`）
- 対象コミット: レビュー時 `b8ad190`。修正後 `dd1036c`
- 走らせた reviewer: spec / rust / robustness / test（4本）
- **未実施: comment / architecture / perf**（セッション枠を使い切って落ちたため、ラウンド2で回す）

前ラウンドは `.claude/reviews/2026-08-30-parser-silent-loss-r1.md`（所見 ID R2-xx）。
このラウンドの所見 ID は R3-xx。

---

## 直したもの

| # | reviewer | 深刻度 | 所見 | 直したコミット |
| --- | --- | --- | --- | --- |
| R3-01 | robustness | **BLOCK** | **このブランチが作った退行。** `retain_mut(\|v\| normalize_moves(..).is_ok())` が正規化できない分岐を戻り値に出さず削除する。`Ok` が返り棋譜が1本短くなる | `f134d9b` |
| R3-02 | robustness | HIGH | 同じ変更で、中断のあとに続く手が中断前の局面から指せないと棋譜全体が `Err` になる | `f134d9b` |
| R3-03 | robustness | **BLOCK** | **KI2 の空行で打ち切る。** R-KI2-002 の実例（12手）が6手として `Ok` | `0289a1e` |
| R3-04 | robustness | HIGH | `変化：` ブロック頭のコメントで以降のブロックが全消え。**自分が書いた KI2 を自分で読めていない** | `0289a1e` |
| R3-05 | robustness | MEDIUM | 読めない持駒行が `information_line_keyvalue` に落ちて `header` に化け、持駒が0になる | `4b87430` |
| R3-06 | spec | MEDIUM | 途中の終局が手数を食うと `反則勝ち` の向きが反転する（読み側だけ偶奇のまま） | `9e9d005` |
| R3-07 | rust | MEDIUM | 手書きループが `nom::Err::Failure` を握り潰す。KIF 側に同じ穴 | `0b8a5e3` |
| R3-08 | rust / robustness | MEDIUM | `まで` 行のエラーが**次の行**を指し、読めなかった語を含まない | `0b8a5e3` |
| R3-09 | rust | MEDIUM | **コメントが嘘。** 「nom のコンビネータが返すパーサは寿命ひとつに固定される」は3行上の `move_line` が反証している | `0b8a5e3` |
| R3-10 | rust | MEDIUM | `try_to_usi_owned` が `ConvertError` を `std::fmt::Error` に潰す。新規 API なのに署名を選び直していない | `dd1036c` |
| R3-11 | test | HIGH | KIF パーサの手番を偶奇に戻しても全テストが通る（効くのは `move_special` の経路だけ） | `e93ec45` |
| R3-12 | test | HIGH | `starting_side` が `data.color` を無視しても通る。先手番の任意局面が全部後手起点になる | `e93ec45` |
| R3-13 | test | HIGH | KI2 の手数カウンタを無条件加算に戻しても通る（KIF 側だけテストがあった） | `e93ec45` |
| R3-14 | test | HIGH | `normalize_moves` / `populate_relative_moves` の `break` を戻しても通る | `dd1036c` |
| R3-15 | test | MEDIUM | `every_handicap_round_trips` が KI2 の結果を `let _` で捨てる。手合割16種の KI2 書き出しが未検証 | `dd1036c` |
| R3-16 | test | MEDIUM | `try_to_usi_owned` の成功側が1文字も検証されていない | `dd1036c` |
| R3-17 | test | MEDIUM | 終局語表の `after_csa: None` が「終局が落ちた」「棋譜ごと落ちた」「指し手が全部消えた」を同一視する | `dd1036c` |
| R3-18 | test | MEDIUM | `Relative::C`（直）の絞り込みを恒真にしても通る | `dd1036c` |
| R3-19 | test | MEDIUM | 持駒の枚数の**受理側**が未検証。枚数を書かなくしても18枚を書けなくしても通る | `dd1036c` |
| R3-20 | spec / test | MEDIUM | 終局語表の doc が「全て `research/` から転記」と言うが、3行は**決定**であって規定に無い | `dd1036c` + D7 |
| R3-21 | spec | MEDIUM | `research/tables/60-to-ki2.md` と `40-vocabulary.md` が HEAD と食い違ったまま | `research/` を更新 |

**このラウンドで見つかった、このブランチが作った退行は2件（R3-01 / R3-02）。**
どちらも `break` の除去に伴うもので、同じコミットで入れた修正の副作用。

## 直していないもの

| # | reviewer | 深刻度 | 所見 | 送り先 |
| --- | --- | --- | --- | --- |
| R3-22 | spec | MEDIUM | 途中の終局のあとに指し手を続ける KI2 を tsshogi が読めない。tsshogi は同じ棋譜を `変化：` ブロックとして書く。**D3（分岐は tsshogi に合わせる）から外れている** | `95-decisions.md` **J15**（未決）。コーパス609件に該当0件 |
| R3-23 | rust | MEDIUM | 深い分岐で `normalize_moves` の再帰がスタックを使い切り **SIGABRT**（2MB で深さ2500、8MB で10000） | `90-gaps.md`「未調査」→ 数字つきで記録。**コーパスの最大深さは 8** で実データの約300倍が必要 |
| R3-24 | rust / spec | HIGH | `to_*_owned` の `#[deprecated]` は強制力が無い。obs-shogi に `deny(warnings)` も `[lints]` も無い | `.claude/plans/v0.4.0-release.plan.md` |
| R3-25 | spec | HIGH | 0.4.0 を載せると `後手番` が2行書かれる（obs-shogi の `patch_gote_start` と二重） | 同上（前ラウンド R2-14 の再掲。リリース手順の必須項目） |

## このラウンドで別途見つけたもの（reviewer 由来ではない）

**GAP-013**: `normalize()` が駒打ちを盤上の手に書き換える。JKF は駒打ちを `from` を
書かないことで表す（R-JKF-003）のに、normalizer がそれを「移動元が不明」と読んでいた。

- tsshogi 由来の JKF 59件で **21件が `Err`（36%）**、さらに **6件で駒打ち428手が黙って別の手に化ける**
- `origin/main` でも同数なので**上流由来の既存バグ**
- **obs-shogi が write 経路で `normalize()` を呼ばない理由がこれだった**
  （`~/obs-shogi` の `9626600`「writeの際の正規化を削除」）
- 直し方はユーザーに選ばせた（案B）。コミット `41e23bc`
- 解消後: `Err` **0件**、化けた駒打ち **0手**

## 数字

コーパス609件（`/Users/riohatta/Desktop/temp`、KIF → JKF → KI2 → JKF）:

| | レビュー前 | 修正後 |
| --- | --- | --- |
| 指し手 | 86906 → 86906 | **86946 → 86946** |
| 分岐 | 1735 → 1735 | **1736 → 1736** |
| 終局 | 583 → 583 | 583 → 583 |
| 読み戻し不能 | 0 | 0 |

**分岐が1本、指し手が40手増えている。** `retain_mut` が実データからも分岐を消していた。

tsshogi 由来 JKF 59件（`normalize()`）: 失敗 21件 → **0件**、化けた駒打ち 428手 → **0手**。

テスト: 66 → **80**。`bash .claude/verify.sh` green。

## 見ていない範囲（reviewer の申告を統合）

- **comment / architecture / perf の3観点が未実施**（ラウンド2）
- CSA / USI / JKF を入口とする経路。`40-vocabulary.md` 表5 の「未検証」セルは半分残っている
- `data/tests/` の期待 JSON が仕様由来か上流実装の出力か
- 文字コード判別（`.ki2` に `.kif` の UTF-8 再試行が無い）
- 実コーパスは**平手609件のみ**。駒落ち・任意局面・途中終局は現物が無く、確認はすべて合成入力
- `benches/parse.rs` はクリーンチェックアウトで動かない（前ラウンド R2-58、未修正）
- Windows / Linux でのスタックサイズ

## lint / hook / 型で強制できるもの

- **`Ok((_, jkf))` で残り入力を捨てる形**は `all_consuming` か専用ラッパで型に寄せられる。
  robustness の HIGH 2件と BLOCK 1件がこれ1つで `Ok` から `Err` に変わる（**ブランチB の GAP-005**）
- **`retain_mut(|..| ..is_ok())`** を `clippy.toml` の `disallowed-methods` へ。
  「戻り値に出ない削除」を明示的に許可させる。R3-01 はこれで機械的に出た
- **`let _ = <Result>`** を `clippy::let_underscore_must_use` で止める。R3-15 と R3-16 の穴の作られ方
- **`nom::Err::Failure` の握り潰し**は `rg 'let Ok\(.*\) = .*\(input\)' src/parser/` で洗える。3箇所しかない
- `MoveSpecial` の対応表がまだ4箇所（`kif_word` / `csa_word` / `ki2_phrase` / `KIF_SPECIAL_WORDS`）。
  1つの `const TABLE: [Row; 14]` にすれば足し忘れが型で消える
- `clippy::wildcard_enum_match_arm`（前ラウンド R2-25 の対策）は未導入

## 次ラウンドの対象

1. comment / architecture / perf の3観点を回す
2. そこで出た所見を直す
3. 所見0のラウンドが出たら `v0.4.0` のタグとリリース手順（`.claude/plans/v0.4.0-release.plan.md`）へ

# レビュー four-issues ラウンド7

- 日付: 2026-09-01
- 範囲: `git diff main...HEAD`
- 対象コミット: 開始時 `a402f71`（ラウンド6の修正 `6fb71c5` 〜 `cf589bc` が対象）
- 走らせた reviewer: rust / spec / robustness / comment / architecture
- 重複を潰した結果: **BLOCK 3 / HIGH 3 / MEDIUM 8**
- **12件を修正、2件をユーザー判断としてGAPへ、1件を上流由来として記録。** 1所見1コミット（`cac49d2` 〜 `256432f`）。doc の4件だけ1コミットにまとめた

**ラウンド6が持ち込んだ退行が4件**（R7-01 / R7-02 の一部 / R7-03 / R7-04）。
うち3件は reviewer 5本のうち3本以上が独立に踏んだ。

**さらに、ラウンド6の報告書が「直した」と書いて木に入っていなかったものが2件ある**
（R7-08 / R7-05 の一部）。R4-20 と同じ失敗の2回目。

## 所見

### R7-01 [BLOCK] `*` / `&` 行がヘッダブロックを打ち切り、その下のヘッダ行・盤面図・手合割が消える

- 場所: `src/parser/kakinoki.rs:461` `information_line_keyvalue` の門番 / `:487-499` `information_lines`
- 指摘: rust（BLOCK）/ robustness（BLOCK）/ spec（HIGH）— 3本が独立に
- **R6-02 が持ち込んだ。** `Error` を返すと `information_lines` の `many0` が**そこで止まる**。
  `#` には `many0(comment_line)` という逃げ道があるが `*` / `&` には無い。
  「ヘッダ行ではない」と「ヘッダブロックの終わり」を同じ返り値で表している
- 実測（左が `main`、右が `HEAD`。`771cd0f` も `main` と同じ）:

  | 入力 | main | HEAD |
  | --- | --- | --- |
  | `先手：山田` / `*（主催：…）` / `後手：田中` / 指し手 | `Ok` header 3件 | **`Ok` header 1件。`後手` が痕跡なく消える** |
  | `手合割：その他` / `*出典：…` / 盤面図 / `手数----` | `Ok` `preset=OTHER` board あり | **`Err`。ファイルごと読めない** |
  | `*メモ：あ` / `手合割：香落ち` / `   1 ３四歩(33)` | `Ok` `PresetKY` | **`Err failed to normalize: Invalid move: ３三→３四 at ply 1`** |

- **3行目が一番重い。** 手合割を失って読み手の既定である平手に落ち、駒落ちは上手が先に指す
  （R-HC-001 / R-RULE-006）ので1手目が非合法になり、正当な棋譜が索引から丸ごと落ちる
- **書き side が決定的**: `header` のキーが `*` / `&` で始まると、**このクレートが書いたファイルを
  このクレートが読めない**。`write_header` は `HashMap` 順で書く（GAP-022）ので、
  同じ JKF を2回保存すると消える行が毎回変わる
- コーパス609件に該当は0件（`.kif` は `手数----` の下にコメントを置く）。踏むのは `.ki2`
  （区切り行が無い、R-KI2-002）と、`header` にそのキーを持つ JKF
- 直し方: 門番は残す（R6-02 は正しい）。**打ち切りをやめる。** `information_lines` の前置
  `many0(comment_line)` に `*` / `&` も通し、本文は捨てずに `moves[0].comments` へ渡す
  （R-KIF-010: 初手より前のコメントは開始局面のもの）
- **結果**: 直した（`cac49d2`）: 拒否ではなくコメントとして読み、`moves[0].comments` の先頭へ置く。R6-02 の門番は `alt` の並べ替えに対する保険として残した

### R7-02 [BLOCK] 行頭の余白が `SPACES` を引かず、タブで字下げされた `変化：` / `まで…` / 指し手の走りが散文に化ける

- 場所: `src/parser/ki2.rs:280-290` `move_run` の読み飛ばし（`tag(" ")` / `tag("　")` の手書き）/
  `:335` `a_line_only_prose_opens`（`input` を trim せずに `opens_a_shared_line` を聞く）
- 指摘: architecture（BLOCK）/ rust（BLOCK）/ comment（HIGH）— 3本が独立に
- `opens_a_shared_line` は先頭一致しか見ないので、`\t変化：2手` も `\tまで2手で投了` も
  「形の無い行」に落ち、`not_move_line` が行ごと読み飛ばす。
  **`begins_the_line_below` は同じ述語を `trim_start_matches(SPACES)` してから聞いており、
  引き方が食い違っている**
- 実測（`手合割：平手` / `▲７六歩 △３四歩` の下に字下げだけ変えた行を1本）:

  | 字下げ | `まで2手で投了` | `変化：2手` + `▲２六歩` | `▲７六歩 △３四歩`（走り） |
  | --- | --- | --- | --- |
  | 無し / 半角 / 全角 | `Ok` + 投了 | `Ok` fork 1 | `Ok` 2手 |
  | **タブ** | **`Ok`・`special=None`** | **`Ok` 3手・fork 0** | **`Err`** |

- **`\t変化：2手` は `main` が `Err` にしていたのが `Ok` になり、分岐が本譜の3手目に化ける**（R-JKF-004）
- タブ字下げの走りが `Err` になるのは `main` からだが、`move_run` の直上のコメント自身が
  「a run can also arrive indented」と約束している（R-KI2-001、D18）
- R6-05 は「`まで…` の**後ろ**のタブ」を直した。**前のタブは同じ表を引いていない**
- 直し方: `move_run` の `tag(" ")` / `tag("　")` を余白の述語から作り、
  `a_line_only_prose_opens` も `begins_the_line_below` と同じく trim してから聞く
- **結果**: 直した（`fe8f9b7`）: 読み飛ばしを `is_padding` から作り、`a_line_only_prose_opens` も余白の向こうを見る。無し・半角・全角・タブ・NBSP の5通り × 投了・分岐・走りをテストで固定

### R7-03 [BLOCK] R6-08 の「後ろから2番目のマークだけで足りる」が偽。R5 が捕らえていた入力を黙って通す

- 場所: `src/parser/ki2.rs:159-177` `a_header_value_carrying_moves`（doc と実装の両方）
- 指摘: comment（BLOCK）/ rust（MEDIUM）
- **R6-08 が持ち込んだ。** 論証は「1つの指し手はちょうど1つのマークを含む」を前提にしているが、
  `single_move` は `opt(many1(move_comment_line))` を続けて読むので、**最後の指し手が末尾の
  コメントを飲み込み、そのコメントの中のマークが「最後のマーク」になる**
- 実測:

  | 入力 | 771cd0f | HEAD |
  | --- | --- | --- |
  | `手合割：平手 ▲７六歩 △３四歩` | `Err` | `Err` |
  | `手合割：平手 ▲７六歩 △３四歩 *▲２六歩は疑問` | **`Err`** | **`Ok` 0手。`header["手合割"]` に全文** |
  | `手合割：平手 ▲７六歩 △３四歩 &▲２六歩` | **`Err`** | **`Ok` 0手** |

- **doc が証明として書いた不変条件が偽である**ことが値打ち。`single_move` に何かを足す人は
  この doc を読んで「マークと指し手は1対1」と信じる
- R6-08 の速度（19.6 s → 80 µs）は保つ価値がある
- 直し方: 最後のマークから**後ろ向きに、回数の上限を置いて**試す。上限は文書化し、
  超えた場合に何を見逃すかを GAP-020 に書く
- **結果**: 直した（`a13f454`）: マークを後ろから `ATTEMPTS` 個まで試す。上限と、それを超える注記を見逃すことを doc と GAP-020 の5に書いた。3200手1行は 130 µs のまま

### R7-04 [HIGH] `変化：` を認める述語だけ広げたので、`main` が読めていた KIF 133件が `Err` になる

- 場所: `src/parser/kakinoki.rs:136` `opens_a_branch_header`（R6-03 が広げた）に対して
  `src/parser/kif.rs:124` `branch_header_line` と `src/parser/ki2.rs:250` `branch_header`（`digit1`）
- 指摘: robustness（HIGH）/ spec（MEDIUM）
- **R6-03 が持ち込んだ。** 認める側と読む側の集合が食い違い、**その差分の行はどこにも行き場が無い**
- 実測:

  | 入力（`.kif`） | main | 771cd0f | **HEAD** |
  | --- | --- | --- | --- |
  | 本譜2手 + `変化：２手`（全角） + 分岐1手 | `Ok` **分岐1本** | `Ok` **分岐1本** | **`Err`** |
  | 本譜2手 + `変化： 2手`（空白） + 分岐1手 | `Ok` 分岐1本 | `Ok` 分岐1本 | **`Err`** |
  | `   1 ７六歩(77)   ( 0:01/…) 変化：２手を参照` | `Ok` 2手 | `Ok` 2手 | **`Err`（`runs into the one below it`）** |

- **規模**: コーパス609件に `変化：` + 全角数字を1箇所ずつ注入した24,360通りで、
  **`771cd0f` が「元と同じ内容で `Ok`」だった 133 件が HEAD で `Err`**。
  多くは `  83 投了    変化：２手     ( 0:00/00:00:00)` のように**指し手行の注記欄**
- 3行目は**エラー文が嘘をついている**（下の行に食い込んでなどいない）。
  D17 の表がラウンド1〜3で3回同じ嘘をついた場所で、4回目
- KIF では `変化：` 行の中身を1文字も使っていない（D3 規則1）ので、
  **この行を読めないことによる損失は元々ゼロだった**
- 直し方: 2つ同時に。(a) パーサ側を広げて集合を揃える（KIF は数字を捨てるだけ、
  KI2 は全角を半角に畳んで `parse`）。(b) 行末に残った `変化：…` を「下の行」と断じるのを、
  R6-07 が `まで…` に引いたのと同じ線——**その行の末尾まで `変化：<数字>手` で終わっていること**
  ——にする。片方だけでは注記欄の側が残る
- **結果**: 直した（`93eaf03`）: `branch_header_ply` を `kakinoki` に置き、`opens_a_branch_header` と同じ形を消費する（全角数字は半角に畳む）。行末の位置では「行の末尾まで `変化：<数字>手`」を要求。**途中で UTF-8 境界の panic を書き、既存のテストが捕まえた**

### R7-05 [HIGH] 余白が3文字の表のままで、NBSP / THIN SPACE / VT / FF はいまも `Err`

- 場所: `src/parser/kakinoki.rs:84` `SPACES` と、それを引く5箇所
- 指摘: robustness
- **ラウンド6の報告書は R6-05 の実測表に `まで2手で投了\u{a0}`（NBSP）を挙げて
  「結果: 直した（`97cab82`）」と書いたが、`SPACES` に NBSP は入っていない。**
  R4-20 と同じ失敗の2回目
- 実測（`まで2手で投了<pad>`）:

  | pad | main | 771cd0f | HEAD |
  | --- | --- | --- | --- |
  | `' '` / `　` | Ok | Ok | Ok |
  | `\t` | Ok | **Err** | Ok（R6-05 が直した） |
  | **`\u{a0}` NBSP** | **Ok** | Err | **Err** |
  | `\u{2009}` / `\u{b}` / `\u{c}` | **Ok** | Err | **Err** |

- エラーは語彙にある `投了` を指し、原因の見えない1文字は表示にも出ない。
  NBSP は Web からの貼り付けに普通に混ざる。KIF 側は `ends_here` の最後の腕が食うので
  **同じ内容が `.kif` なら読めて `.ki2` なら読めない**（D18 違反）
- 直し方: 余白を3文字の表ではなく述語にする
  （`c.is_whitespace() && !LINE_ENDS.contains(&c)`）。**行末を除く条件は落とせない**
  ——R6-06 が塞いだ穴が開き直る
- **結果**: 直した（`b2d9c8d`）: `SPACES` を `is_padding(c) = c.is_whitespace() && !LINE_ENDS.contains(&c)` に置き換え。テストは表の列挙をやめ、`is_padding` が真であることを assert してから7種を回す

### R7-06 [HIGH] `to_ki2` が自分の読み手が拒む `.ki2` を書く【ユーザー判断が要る】

- 場所: `src/converter/kakinoki.rs` `write_header` と `src/parser/ki2.rs`
  `a_header_value_carrying_moves`
- 指摘: spec
- 実測: `header = {"note": "▲７六歩 △３四歩"}` の JKF → `try_to_ki2_owned` →
  `"note：▲７六歩 △３四歩\n手合割：平手\n"` → `parse_ki2_str` = **`Err`**。
  同じ JKF を `to_kif` → `parse_kif_str` は `Ok`。**`main` では KI2 も `Ok`**（R5-08 由来）
- consumer への筋道: obs-shogi の新規作成フォームが `note` を `header` に入れる。
  利用者が note 欄に指し手を2手書いて `.ki2` で保存すると、**そのファイルだけ二度と開かない。
  新規作成なので原本も無い**
- **直し方に設計の判断が要るので、このラウンドでは直さない。** 選択肢:
  - (a) 書き手が読み手と同じ判定を引き、真なら `ConvertError` で拒む（**保存できなくなる**。D4 に反する）
  - (b) 書き手が値を綴り替える（可逆でない加工。原本を壊す）
  - (c) 読み手の判定を緩める（R5-08 が塞いだ「改行を失った KI2 が全部ヘッダに入る」が戻る）
  - (d) 現状を限界として GAP に記録し、consumer 側で note にヘッダを書かせない
- **GAP-020 に記録した。ユーザーに選んでもらう。**
- **結果**: **直していない。GAP-028 に記録し、ユーザーに選択肢 (a)〜(d) を出した。**

### R7-07 [MEDIUM] `▲` / `△` を含む1行の散文が `.ki2` ではファイルごと `Err`【ユーザー判断が要る】

- 場所: `src/parser/ki2.rs:335-350` `a_line_only_prose_opens` の `line.contains(SIDE_MARKS)`
- 指摘: spec
- **`main` も同じなので、このブランチの退行ではない**
- 実測（指し手の後ろに1行置く。左が `.ki2`、右が同内容の `.kif`）:

  | 行 | `.ki2` | `.kif` |
  | --- | --- | --- |
  | `※▲２六歩が本筋` / `（▲７六歩まで）` / `感想：▲有利でした` | **`Err`** | `Ok` |
  | `消費時間：104▲379△380` | **`Err`** | `Ok` |

- **同じクレートの中で判断が割れている。** `what_counts_as_the_line_below` は
  `(ki2::SHAPES, "（△８四歩 ▲２六歩）")` を「注記であり、捨てても何も失わない」として
  固定しているのに、同じ文字列が1行として置かれるとファイル全体が読めない
- `消費時間：104▲379△380` は `a_header_value_carrying_moves` の doc が
  「実在のヘッダ」として名指ししている綴りそのもの
- 直し方: 判定を `a_move_run_fills_the_line` と同じ線に寄せる。
  **ただし散文を捨てる側に倒すので、`▲` だけの行が本当に指し手だった場合は静かに消える。**
  D18 に行を足してから動かすこと → **GAP に記録し、ユーザーに選んでもらう**
- **結果**: **直していない。GAP-029 に記録し、ユーザーに選択肢 (a)/(b) を出した。** `main` も同じで退行ではない

### R7-08 [MEDIUM] ラウンド6の GAP-024 更新が木に入っていない

- 場所: `research/90-gaps.md` GAP-024 の表と `src/parser/kakinoki.rs:76-78` の doc
- 指摘: comment
- doc は「GAP-024 names every place that has to learn a new one together — the KI2 writer
  among them」と書くが、**現物の表は3行で書き手を1行も持たない。**
  しかも `not_move_line` の行は `none_of` 直書きのまま（R6-17 で `satisfy` に変えた後の姿と違う）、
  `parser/ki2.rs` `not_readable_line` の行は **R6-01 で消えた関数**を指している
- 原因: ラウンド6でこの編集を `git commit` と同じ呼び出しに入れ、
  **ゲートが呼び出し全体を拒否したため python が走らなかった**（`MEMORY.md` に記録済みの癖）
- **R4-20 と同じ失敗の2回目。** 所見を閉じる前に木を確かめること
- **結果**: 直した（`93eaf03` に同梱の `research/` 編集）: GAP-024 の表を現状に合わせ、書き手（`converter/ki2.rs`）の行を足した。`research/` は gitignore 済みなので差分には出ない

### R7-09 [MEDIUM] `move_word` / `board_word` の doc が引く要件 ID が、そこで主張していることを言っていない

- 場所: `src/notation.rs:64-67` と `:93-97`
- 指摘: comment
- R-KI2-005（`research/31-ki2.md`）の全文は「R-NOT-006 の揺れはすべて KI2 に現れうる。
  **読み取りでは受けること。**」で、指し手と盤面図で書き出しの表が分かれることには何も言っていない
- R-NOT-006 の表では `杏` `圭` `全` は**揺れの側**で、正規形は `成香` `成桂` `成銀`。
  **R-NOT-006 だけを読むと `board_word` は正規形でないものを書いていることになる**
- 盤面図が1マス2文字であることの根拠は R-KIF-014 / D6。次にこの表を触る人が示された ID を
  確かめに行くと**書いてあるのは逆のこと**なので、`board_word` を `成香` に「直して」桁を壊せる
- 直し方: `move_word` は R-NOT-006、`board_word` は R-KIF-014 / D6 を引く。
  R-KI2-005 は読み取り側（`piece_kind`）に付ける
- **結果**: 直した（`256432f`）: `move_word` は R-NOT-006、`board_word` は R-KIF-014 / D6 を引く

### R7-10 [MEDIUM] ラウンド6が書いた doc に、このリポジトリの過去の欠陥が主語として入っている

- 場所: `src/notation.rs:70-73` と `src/parser/kakinoki.rs:1000-1002`
- 指摘: comment
- 「Two of them, hand-written, is how `to_kif` came to write `龍` and `to_ki2` `竜`」。
  いまはどちらも `move_word` を引いて `龍` を書く
- **R1-09 / R4-05 / R6-14 と同じ形が4回目**で、今回は直した本人の説明文として入っている。
  読み手は `to_ki2` を開いて `竜` を探し、無いことに戸惑う
- 直し方: 過去形を落として現在の契約だけにする。`龍`/`竜` の事実は `git log` と R6-12 にある
- **結果**: 直した（`256432f`）

### R7-11 [MEDIUM] テストの説明が「別の経路で捕まる」と言うが、実際は同じ検査・同じ行で落ちる

- 場所: `src/parser/ki2.rs:666-669`
- 指摘: comment
- 4通りすべて `at line 3, in cannot read this: 変化：…`。「moves under the header を通って」
  捕まる経路は無い
- 2行の表がなぜ2行あるかの説明が事実と違うので、次に整理する人が片方を消す。
  **区別は現在の経路ではなく、退行したときの壊れ方にある**（`▲` の側は分岐の1手目が本譜に化け、
  `まで…` の側は終局が本譜に付く）
- **結果**: 直した（`93eaf03`）: R7-04 の修正で `変化：２手` が読めるようになったので、テストごと「4通りの綴りが同じ木を作る」に書き換えた

### R7-12 [MEDIUM] `LineShapes` の doc の「this module holds only what they share」はまだ偽

- 場所: `src/parser/kakinoki.rs:96-98` に対して `:234` `blank_line`（読み手は `kif.rs` だけ）と
  `:239` `program_comment_line`（読み手は `ki2.rs` だけ）
- 指摘: architecture
- 「空行とは何か」を KIF は `blank_line`（`space0` + 改行）で、KI2 は `move_run` の
  `many0(alt((line_ending, tag(" "), tag("　"))))` で答えている。
  **同じ問いに2つの実装があること自体が R7-02 の正体**
- **結果**: 直した（`256432f`）: doc を実態に合わせ、`blank_line` の `space0` を `is_padding` にした

### R7-13 [MEDIUM] `notation::MoveText` の駒打ちが `board_word` を引いている

- 場所: `src/notation.rs:186`
- 指摘: architecture
- doc 自身が「A drop does carry it, and `７六歩打` is what a file would say」と言うのに、
  **指し手の綴りに盤面図の表を引いている**。成香の駒を持つ壊れた `Drop` で `杏打` と出る
- R6-12 で作ったばかりの区別が、新設の直後に1箇所で守られていない（エラー文だけなので実害は小さい）
- **結果**: 直した（`256432f`）

### R7-14 [MEDIUM] `notation::pk2k` が `shogi_core/into.rs` の14行と同じ表

- 場所: `src/notation.rs:194-211` と `src/shogi_core/into.rs:15-33`
- 指摘: architecture
- **`main` から在るもので、このブランチが持ち込んだものではない。** `notation.rs` の doc は
  「How a square and a piece are spelled」と名乗っており、`pk2k` は綴りではなく型変換
- **上流との差分を小さく保つため、このラウンドでは触らない**（`90-gaps.md` に記録）
- **結果**: **直していない。GAP-030 に記録。** `main` から在るもので、上流との差分を小さく保つため触らない

## reviewer が挙げたが所見にしなかったもの

- **`not_move_line` の `satisfy` から `SPACES` を除く案**（rust）: KIF でタブ字下げの散文が
  誰にも読まれなくなる恐れがある。R7-02 は `move_run` 側と述語側だけで解ける
- **`single_move` から末尾のコメント読みを外す案**（rust）: `move_run` と
  `a_move_run_fills_the_line` の両方に波及するので範囲外
- **`opens_a_branch_header` を `is_numeric()` にする案**: 漢数字・丸数字まで入り、
  パーサ本体との差が広がる。`('０'..='９')` が正しい（rust が確認）
- 公開 API の差分なし（`git diff 771cd0f..HEAD` で `pub` の増減 0。
  `main` との公開項目の集合が md5 一致、34項目）
- panic 経路なし（ラウンド6の差分に `unwrap` / `expect` / `panic!` の追加なし。
  1,255通り + 40万件の fuzz で panic 0件）
- コーパス609件の読み書きは `main` / `771cd0f` / `HEAD` で**609行すべて同一**
- R6-12 の消費時間の桁合わせは全14種で旧実装と同じ幅（spec が golden と突き合わせて確認）

## 数えた結果（architecture、R6 の表との比較）

| 知識 | R6 | **R7** |
| --- | --- | --- |
| 手番記号 | 3 | **2** |
| 「これは `変化：` の行か」 | 3 | **1** |
| 駒種の表記 | 4 | **4**（`write_hand` が数えられていなかった） |
| 行末の集合 | 2 | 2（nom の `line_ending` に理由あり） |
| 終局語 | 3 | 3（`converter.rs` の往復表が機械的に固定） |
| **行頭の余白とは何か** | 未計測 | **6** |

## 見ていない範囲

- `.ki2` / `.csa` / `.jkf` の実コーパスが1件も無い。KI2 の所見はすべて合成入力
- obs-shogi はソース読みのみ。このブランチを指してビルドしていない
- CSA 経路はラウンド6の差分に無いため誰も読んでいない
- 深い入れ子の `forks`（GAP-019）、`normalize` の冪等性、USI / JKF を入口とする経路
- 盤面図ブロックと持駒行の境界値

## 修正後の検証

- `bash .claude/verify.sh` — 通る（テスト157件。ラウンド6終了時は153件）
- コーパス609件（`~/Desktop/temp`）の読み書き:
  - **ラウンド5終了時（`771cd0f`）とバイト一致**（609行すべて同じ）
  - `main` との差は意図した分だけ: 読み取り 0件 / `to_kif` 0件 /
    `to_ki2` 1件（`bug_mega.kif`、R-NOT-005）/ `to_csa` 33件（GAP-023）
  - **R7-04 が戻した133件は、この長さ比較には出ない**（注入した綴りはコーパスに無い）。
    reviewer の注入実験の再現は次ラウンドの robustness に任せる
- **R7-04 の途中で `branch_header_ply` が UTF-8 境界で panic する版を書いた。**
  変換後のバイト長で元の文字列を切っていたもので、既存の
  `what_counts_as_the_line_below` が即座に落として捕まえた

## ユーザーに判断してもらうもの（2件）

| | 内容 | 記録 |
| --- | --- | --- |
| **R7-06** | `to_ki2` が自分の読み手が拒む `.ki2` を書ける（`header` の値が指し手2つで終わる形）。**このブランチで入った**（R5-08 由来）。新規作成のファイルが二度と開かない | GAP-028 に選択肢 (a)〜(d) |
| **R7-07** | `▲` を含む1行の散文が `.ki2` でだけファイルごと `Err`（D18 違反）。`main` も同じ | GAP-029 に選択肢 (a)/(b) |

## research/ へ書き戻したもの

- **GAP-024 の表を現状に**（ラウンド6で木に入っていなかった分。R7-08）
- **GAP-020 に5つ目**: `ATTEMPTS` の上限を超えるマークがあると検出から外れる（R7-03）
- **GAP-028 を新設**: 書き手が読み手の拒む `.ki2` を書ける（R7-06、**ユーザー判断**）
- **GAP-029 を新設**: `▲` を含む散文が `.ki2` で `Err`（R7-07、**ユーザー判断**）
- **GAP-030 を新設**: `notation::pk2k` が `shogi_core/into.rs` と同じ表（R7-14、`main` 由来）

## 次ラウンドの対象

ラウンド7の修正。特に:

- ヘッダブロックが `*` / `&` をコメントとして読むようになったこと（R7-01）の波及
  ——盤面図の前後で `informations` が2回走ること、`moves[0].comments` の順序
- `is_padding` が `is_whitespace()` になったこと（R7-05）の波及。
  U+2028 / U+0085 のように「空白だが行区切りでもある」文字の扱い
- `branch_header_ply`（R7-04）——数字の桁あふれ、`変化：` の後ろの余白
- `a_header_value_carrying_moves` の `ATTEMPTS`（R7-03）が実在の綴りで足りるか

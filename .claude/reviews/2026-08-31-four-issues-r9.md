# レビュー four-issues ラウンド9

- 日付: 2026-09-02
- 範囲: `git diff main...HEAD`
- 対象コミット: 開始時 `f7d2648`（ラウンド8の修正 `bf2da21` 〜 `8b6a878` が対象）
- 走らせた reviewer: rust / spec / robustness / comment / architecture
- 重複を潰した結果: **BLOCK 3 / HIGH 4 / MEDIUM 10**

**ラウンド8が持ち込んだ退行が5件**（R9-01 / R9-02 / R9-04 / R9-05 / R9-16）。
根はどれも同じ——**R8-01 が「行頭の余白」を読み飛ばす側にだけ入れ、読む側に入れなかった**こと、
および **R8-05 が半角コロンを広げた先を確かめなかった**こと。

## ラウンド8の検証（robustness の実測）

- コーパス注入 22綴り × 4リビジョン（29,834通り／綴り）。`e10c2a9 → HEAD` の
  **`→SHORTER` は0件**。`SAME→ERR` は2種のみで、どちらも意図した分
  （`変化:2手` を行末に 26,493件＝D17 のとおり、`手合割:香落ち` を挿入 282件＝R8-05 が
  手合割を読むようになり平手のコーパスが非合法になるため）
- 利得側: `ERR→SAME` 25,174 + `SHORTER→SAME` 1,487（全角字下げ）、
  `後手の持駒：なし ` の挿入で `ERR→SAME` 2,973
- 80万パースで **panic 0件**。速度はコーパス609件で 144.7ms（main）→ 149.0ms（HEAD）＝**+3%**
- ラウンド8の変更はすべて**変異を当てると落ちるテストを持っている**
  （飽和を外すと2件、`not_move_line` を列0に戻すと11件、`colon` を全角に戻すと1件、ply 0 で1件）

## 所見

### R9-01 [BLOCK] 見出しブロックだけが「行頭の余白」を知らない。字下げされた盤面図・持駒・手番が `Ok` のまま消える

- 場所: `src/parser/kakinoki.rs:553` `information_line_preset` / `:563` `information_line_hands` /
  `:764` `side_to_move_line` / `:703` `board` / `:622` `information_line_keyvalue` の
  `is_not("：\r\n")`（キーに余白が焼き付く）
- 指摘: spec（BLOCK）/ robustness（BLOCK）/ rust（HIGH）/ comment（MEDIUM）— **4本が独立に**
- **R8-01 が持ち込んだ。** 読み飛ばす側（`not_move_line`）だけを余白に対応させたので、
  **読む側が取れなかった行を読み飛ばす側が黙って持って行く**
- 実測（`main` / `e10c2a9` / `HEAD`）:

  | 入力 | main | e10c2a9 | **HEAD** |
  | --- | --- | --- | --- |
  | 盤面図の局面、`後手番` の前に半角空白1つ | **`Err`** | **`Err`** | **`Ok`・`color=Black`** |
  | 盤面図の全行を半角空白1つ字下げ | **`Err`** | **`Err`** | **`Ok`・`board=なし`・平手に落ちる** |
  | 同、`\t` / `　` の字下げ | `Ok`・board 消失 | 同 | 同 |
  | ` 手合割：香落ち` + 1手 | `Ok` 平手 | 同 | 同 |
  | ` 後手の持駒：飛` | `Ok`・持駒0・`header[" 後手の持駒"]` | 同 | 同 |

- **上2件はこのブランチで `Err` から沈黙へ落ちた退行。** 4行目は `header` のキーが `" 手合割"` に
  なるので **D16 の `is_a_known_name` が効かず**、書き戻すと `手合割` の行が2本並ぶ:

  ```
   手合割：香落ち      ← 誰も書いていないキー
  手合割：平手        ← D16 の contains_key が外れて2本目
  ```

- consumer への筋道: 字下げされた `.kif`（Web やワープロ経由。**R8-01 自身がその前提で `is_padding` を
  入れた**）を obs-shogi が読む → 局面が平手に化ける → `to_kif_owned` で保存すると
  **別の局面が原本を上書きする**（R-REQ-002 / D4）
- 仕様: R-KIF-014 / D6、R-HC-001 / R-RULE-006、R-REQ-004
- コーパス609件に該当0行。合成入力のみ
- 直し方: `information_line_preset` / `information_line_hands` / `side_to_move_line` / `board` /
  `information_line_keyvalue` を `preceded(take_while(is_padding), …)` で始める。
  **片方だけ入れないこと**——それが今の状態。回帰は
  `kif_reads_a_line_however_far_in_it_starts` に**行種の直積**（余白6種 × 行種11種）を足す

### R9-02 [BLOCK] 指し手より前の `変化:` が KI2 の棋譜を丸ごと飲み、`Ok` 0手が返る

- 場所: `src/parser/kakinoki.rs:185` `opens_a_branch_header`（R8-05 が `[：:]` に広げた）と
  `src/parser/ki2.rs:407-412` `attach_branch` の裸の `return`
- 指摘: robustness
- **R8-05 が持ち込んだ。** 全角が無事なのは**偶然**で、`変化：2手` は
  `information_line_keyvalue`（全角のみ）に食われて `header["変化"]="2手"` になる。
  半角にはその受け皿が無い
- 実測（`.ki2`、本譜3手）:

  | 入力 | main | e10c2a9 | **HEAD** |
  | --- | --- | --- | --- |
  | `先手：A` / `変化:2手` / 3手 | `Err` | `Ok` 3手 | **`Ok` 0手** |
  | `先手：A` / `*まえがき` / `変化:2手` / 3手 | `Err` | `Ok` 3手 | **`Ok` 0手** |
  | `先手：A` / `変化：2手`（全角） / 3手 | `Ok` 3手 | `Ok` 3手 | `Ok` 3手 |

- **この形は実在する。** tsshogi の `kakinoki.mjs:564-572` が shogihome issue #570 を引いて
  「ヘッダー部に `変化：` で始まる行が存在する場合がある。指し手が始まるより前に出現しても
  無視しなければならない」と明記し、`linePatterns` の BRANCH は `/^[ 　]*変化[：:]/` で
  **半角も拾ったうえで無視する**
- **R8-03 は同じ関数の同じ `return` を理由に ply 0 を拒否したのに、R8-05 が新しい綴りを
  その入口へ通した**
- 直し方: `ki2::moves` が本譜を読む**前**の `変化：`/`変化:` 行を tsshogi と同じく無視して読み進める。
  少なくとも `attach_branch` が置き場を見つけられなかったことを戻り値で表し、
  `moves` が `broken_line` にする（`branch.is_empty()` のときは既にそうしている）

### R9-03 [BLOCK] `single_move` の指し手間の区切りだけ `space0` のまま。全角区切りの走りが「行」と認められない

- 場所: `src/parser/ki2.rs:62` `tuple((space0, opt(line_ending)))`。波及先は
  `a_header_value_carrying_moves`（`:181`）と `a_move_run_fills_the_line`（`:129`）
- 指摘: architecture
- 走りそのものは `move_run` の `take_while1(is_padding)` が拾うので読める。壊れるのは
  **`many1(single_move)` を直接呼ぶ2つの述語**で、そのどちらも「改行を失った行」を捕まえる側にある
- 実測:

  | 入力 | main | **HEAD** |
  | --- | --- | --- |
  | `手合割：平手 ▲７六歩 △３四歩`（半角） | `Ok` 0手 | `Err` |
  | `手合割：平手 ▲７六歩　△３四歩`（**全角**） | `Ok` 0手 | **`Ok` 0手。`header` に全文** |
  | `まで2手で中断 ▲７六歩 △３四歩`（半角） | `Err` | `Err` |
  | `まで2手で中断 ▲７六歩　△３四歩`（**全角**） | **`Err`** | **`Ok` 2ノード。2手が痕跡なく消える** |

- 最後の行が退行。**区切り文字1つで「拒否」と「沈黙」が入れ替わる**
- tsshogi の KI2 読み手は `text.replaceAll(/[\s　]/g, "")` で全ての空白を落としてから割る
- 直し方: `ki2.rs:62` の `space0` を `take_while(is_padding)` に。
  **1行で上表の全角・NBSP が他の綴りと揃うことを architecture が worktree で確認済み**（162件通過）

### R9-04 [HIGH] `not_move_line` が余白の向こうの数字を指し手行と断じ、数字で始まる注記でファイルごと `Err`

- 場所: `src/parser/kakinoki.rs:410-415`
- 指摘: spec
- **R8-01 が持ち込んだ。** 数字の除外は KIF の指し手行を守るためにあるが、
  いま当たっているのは「余白の向こうの1文字目」
- 実測（本譜2手の下に1行）:

  | 置いた行 | main KIF | **HEAD KIF** |
  | --- | --- | --- |
  | `　1図以下、先手優勢` | `Ok` 2手 | **`Err`** |
  | `　3手目は疑問` | `Ok` 2手 | **`Err`** |
  | `\t35手目まで` | `Ok` 2手 | **`Err`** |

- `1図以下` `35手目まで` は KIF の注記としてありふれた綴り
- 直し方: 1文字目が数字かではなく、**その行が KIF の指し手行の形か**を聞く。
  `kif::opens_a_numbered_line`（数字 → 余白 → 中身、という形を既に持っている）を
  `LineShapes` にもう1本足して `not_move_line` に渡す

### R9-05 [HIGH] `変化：2手を参照` が分岐ヘッダとして消費され、`a 変化 block with no moves under it` でファイル全体が `Err`

- 場所: `src/parser/kakinoki.rs:185`（数字1文字で行と断じる）→ `src/parser/kif.rs:511` / `src/parser/ki2.rs:455`
- 指摘: spec
- **R8-01 / R8-05 の系。** 行頭では「数字が1文字あれば分岐宣言」なので、`変化：` で始まる**注記**が
  分岐ヘッダとして消費される
- 実測（本譜2手の下に1行）:

  | 置いた行 | main KIF | **HEAD KIF** | main KI2 | **HEAD KI2** |
  | --- | --- | --- | --- | --- |
  | `変化：2手を参照` | `Ok` 2手 | **`Err`** | `Ok` 2手 | **`Err`** |
  | `変化：2手が有力` | `Ok` | **`Err`** | `Ok` | **`Err`** |
  | `変化：ここから` | `Ok` | `Ok` | `Ok` | `Ok` |

- **D17 の表は `変化：2手を参照` を「注記であり、捨てても何も失わない」として固定しているのに、
  同じ文字列が行頭に来たときだけ扱いが割れている**
- 直し方: 行頭で分岐ヘッダと認めるのを「数字（と任意の `手`）の直後が**余白・行末・`NOTE_MARKERS`**の
  いずれか」に限る。`opens_a_branch_header` と `branch_header_ply` は同じ集合でなければならない
  （R7-04 / R8-02 の不変条件）ので、条件は両方が通る1箇所に置く

### R9-06 [HIGH] `手合割:詰将棋`（半角コロン + 表に無い値）は `header` にも残らず行ごと消える

- 場所: `src/parser/kakinoki.rs:553` と `:623`
- 指摘: rust
- 半角コロンの `手合割` 行は `information_value_preset` が名前を見つけられないと抜け、
  `information_line_keyvalue` は全角を要求するので受けられず、**散文の読み飛ばしに落ちる**
- 実測:

  | 入力 | 結果 |
  | --- | --- |
  | `手合割：詰将棋` | `Ok` `header["手合割"]="詰将棋"` |
  | **`手合割:詰将棋`** | **`Ok` `header` 空。行が消える** |
  | **`手合割:香落ち（30分）`** | **`Ok` `header` 空** |

- **R8-05 の報告自身が「`手合割` の文字列すら残らないほうが重い」と書いている。**
  直したのは軽いほう（表にある名前）で、重いほう（GAP-021 が未決にしている値の集合）が残った
- 直し方: キーが `handicap::KIF_KEYWORD`（と `…の持駒`）のときだけ
  `information_line_keyvalue` にも `colon` を使わせる。`{"header":…}` を弾き続けたまま両立する

### R9-07 [HIGH] `colon` の doc の「as tsshogi does」が偽。半角コロンのヘッダ行は消える

- 場所: `src/parser/kakinoki.rs:119-126`
- 指摘: robustness（HIGH）/ comment（MEDIUM）
- tsshogi の現物は `const metadataPrefix = line.match(/^[^ ：:]+[：:]/);`
  （`kakinoki.mjs:216`）で、**一般のメタデータ行でも半角コロンを受ける**
- 実測: `棋戦：第83期順位戦` → `header` に残る。**`棋戦:第83期順位戦` → `header` 空、キーごと消える**
- **一般の key-value を広げないという判断自体は正しい**（`{"header":{},"moves":[{}]}` が
  ヘッダ1件の棋譜になり、D1 / D8 のエラーが誰にも届かなくなる。実測でテストが落ちて分かった）。
  **その根拠が tsshogi ではないだけ**
- **R6-10 / R7-08 と同じ「検証できる主張が偽」の3回目**
- 直し方: doc から「as tsshogi does」を落とし、根拠を D1 / D8 に置く。
  そのうえで半角コロンのヘッダ行が消えることを `90-gaps.md` に実測付きで記録する

### R9-08 [MEDIUM] 「コロンとは何か」が `変化` だけ2箇所にあり、doc が両者の等式を保証している

- 場所: `src/parser/kakinoki.rs:187`（`strip_prefix(['：', ':'])` の直書き）と `:219`（`colon` を引く）
- 指摘: architecture
- 今日は同じ集合なので出力差は無い。**壊れ方はこの pair が過去2ラウンドで実際に起こしたもの**——
  R6-03（判定だけ広げた）で `変化：２手` が本譜に化け、R7-04（消費だけ狭いまま）で 133件が `Err`
- 直し方: `opens_a_branch_header` を `branch_header_ply(head).is_ok()` にすれば
  「数える集合 = 読める集合」が**構造で**成り立つ

### R9-09 [MEDIUM] `skippable_line` の doc が `padding` に付き替わった（**5回目**）

- 場所: `src/parser/kif.rs:134-152`
- 指摘: comment / architecture / rust — **3本が独立に**
- `ee58b7a` が既存 doc の**直後・空行なし**に `padding` を差し込み、6行が1つの `///` に融合した。
  `padding` のページに `変化：` ブロックの説明が載り、`skippable_line` は doc を1行も持たない
- **R3-17 / R4-04 / R6-13 / R8-08 と同じ故障の5回目。R8-08 を直したのと同じラウンドで入った**
- R8-06 で足した `cargo doc -D warnings` は**この形を素通しする**（リンクは壊れていない）
- 機械化するなら `#![warn(clippy::missing_docs_in_private_items)]`（今回の形だけは落ちる）

### R9-10 [MEDIUM] `not_move_line` の doc が「空白で始まる行には手を出さない」と言い続けている

- 場所: `src/parser/kakinoki.rs:383-386`（doc）と `:401-415`（コード）
- 指摘: comment / rust
- R8-01 は述語から `' '` を落とし、入力を trim してから聞くように変えた。
  **この関数はもう空白で始まる行を declines しない。逆に、余白を飛び越えて中身を見る**
- **同じ関数の中で doc と3行下のコメントが正反対のことを言っている**

### R9-11 [MEDIUM] コードに `R8-02`（レビュー番号）が漏れ、その判断を持つ D19 はどこからも引かれていない

- 場所: `src/parser/ki2.rs:768-770`。D19 を引くべきは
  `kakinoki.rs:206-216` / `ki2.rs:260-272` / `:322-327` / `:737-745`
- 指摘: comment
- `grep -rn "D19" src/` は **0件**。`kakinoki.rs:210` の
  「Real records need three digits; the ceiling is here so that a damaged one is not fatal」は
  D19 の本文そのものだが、出典が無いので**この文だけが要件の主張をしている**
- レビュー番号は `.claude/reviews/` の中にしか無く、読み手はそこから何も辿れない（`research/` が正典）
- 併せて: `a_branch_that_departs_from_no_ply_is_refused_rather_than_dropped` の巨大 ply の腕は
  `is_ok()` しか見ておらず、**その分岐が黙って消えることを1行も書いていない**
  （実測 `変化：18446744073709551616手` → `Ok` nodes=4 forks=**0**）。テスト名とも食い違う

### R9-12 [MEDIUM] 字下げの根拠に `R-KIF-005` を引いたが、あの規定は指し手の文法で桁の話が無い

- 場所: `src/parser/kif.rs:145` / `src/parser/kakinoki.rs:402` / `:935`（いずれもラウンド8の追加行）
- 指摘: comment
- R-KIF-005 の全文は `<指し手> = [<手番>]<移動先座標><駒>[<装飾子>]<移動元座標>` と `例: ２三歩成(24)`。
  手数の欄も行頭の空白も消費時間も出てこない。引用している綴り
  `   1 ７六歩(77)   ( 0:01/00:00:01)` は **R-KIF-008 の例文そのもの**
- **R1-10 → R4-03 → R7-09 と3回出た「ID は実在するが、その ID はそれを言っていない」の再発**
- 直し方: 3箇所とも `R-KIF-008`（または隣に倣って `R-KIF-005 / R-KIF-008`）にする

### R9-13 [MEDIUM] `the_extension_chooses_which_encoding_is_tried_first` が順序を1つも検査していない

- 場所: `src/parser.rs:656-701`
- 指摘: comment
- **変異で確認**: `decode_kifu` の `for encoding in [first, second]` を `[second, first]` に反転しても
  **162件すべて通る**
- R8-09 でこの名前と doc を書き直した動機が「拡張子は**最初に試す**ものを決める」だった。
  その中心の主張が公開 doc と名前に入り、**テストだけが空**
- 直し方: 名前を実態に寄せるか、両方の復号が成功するバイト列を1本入れて
  `.kif` と `.kifu` で違う文字列が返ることを固定する

### R9-14 [MEDIUM] 値が空の持駒行（`後手の持駒：`）でファイル全体が `Err`

- 場所: `src/parser/kakinoki.rs:480-515` / `:580` の `Failure`
- 指摘: spec / robustness — 2本
- **`main` も同じ。退行ではない。** ただし **R8-04 が `なし ` を直したときに引いた根拠が
  そのままこの形に当たる**。tsshogi の `readHand` は `if (!section || section === "なし") continue`
  で**空の値を空の持駒として読む**
- 実測: `なし` / `歩` / `歩十八` / `歩 角` は `Ok`。**空 / `' '` / `'　'` は `Err ... in Tag:`**
- 盤面図を持つ棋譜がファイルごと落ち、エラーは原因を指さない
- 直し方: 「余白のみ → `Hand::default()`」の腕を足す

### R9-15 [MEDIUM] `同　　銀(31)`（全角空白2つ）を拒む

- 場所: `src/parser/kakinoki.rs:752` `opt(tag("　"))`
- 指摘: architecture
- **`main` も同じ。退行ではない。** tsshogi の `moveRegExp` は `同　*` と `　*` で
  **0個以上**。「1個だけ」という規定はどこにも無い
- 実測: `   4 同　　銀(31)` と `   3 ２二角　成(88)` が**ファイル全体で `Err`**。tsshogi は `Ok`
- 桁揃えで空白が2つ入った形。**実物の綴りを1件も確認していない**ので、
  R-NOT-006「読み取りでは受ける」を根拠に足すか GAP に記録するかは判断の余地がある

### R9-16 [MEDIUM] `　▲有利でした` が `main` の `Ok` から `Err` に（GAP-029 の判断が付く前に片側だけ寄せた）

- 場所: `src/parser/kakinoki.rs:400-416`
- 指摘: rust
- 以前は行頭の `\t` や `　` を `satisfy` 自身が受理していたので、その先に `▲` があっても
  行は読み飛ばされていた
- 実測: `\t▲有利でした` / `　▲有利でした` が `main` の `Ok` 2ノードから **`Err`** に
- **GAP-029 がまさにこの判断をユーザー待ちにしている。** R8-01 はその判断が付く前に
  字下げされた行だけを片側へ寄せた
- 直し方: 現状を意図として残すなら doc とテストで固定する。**いまはどちらの記述も無い**

### R9-17 [MEDIUM] R8-01 の代償が測られていない。単文字変異の「`Ok` なのに元より短い」が 3→12 / 9→36

- 場所: `src/parser/kakinoki.rs:400-417`、`research/90-gaps.md` GAP-020 の1
- 指摘: robustness
- 実測（ラウンド8と同じ題材・同じ6通り置換 × 全文字位置）:

  | 題材 | `e10c2a9` | **`HEAD`** |
  | --- | --- | --- |
  | KIF 5手 (1,146通り) | 3 | **12** |
  | KIF 5手+変化 (774通り) | 9 | **36** |
  | KI2 5手 (192通り) | 6 | 6 |

- 増えた分はすべて「走りの最後の指し手行の字下げ3列か手数の桁が壊れた場合」で、
  `Err` だったものが `Ok` 4手（5手目が消える）になる
- **R8-01 の利得（`ERR→SAME` 25,174件）は本物だが、同じ変更が「壊れた行を黙って飲む」面も4倍にしている。**
  この取引を記録しないと、次に読み飛ばしを広げる人は片側の数字だけを先例にする
- 直し方: コードは変えない（散文と壊れた行は内容から区別できない、が GAP-020 の結論）。
  **GAP-020 の1にこの実測表を足す**

## reviewer が挙げたが所見にしなかったもの

- `U+2028` / `U+0085` を行区切りに使ったファイルが `main` の「`Ok` 1手＝黙って欠ける」から
  `Err` になった。**沈黙から拒否への変化**
- `後手の持駒：歩十八 歩十八` は `Ok`（FU=36）だが `to_kif` が `UnspellableNumber(36)`。
  実物の綴りの当てが無い
- `変化：18446744073709551616手` が KI2 で `Ok`・分岐が消える。**D19 が GAP-018 に合流させた分**
- KIF で `▲`/`△` **始まり**の散文行がファイル全体 `Err`。GAP-029 の裏面
- 公開 API の差分なし（`pub` の集合が `main` と**完全一致、82項目**）
- `Cargo.toml` は `main` とバイト同一。追加ファイルは `.claude/reviews/` の8件のみ。
  `src/` は production 4,906 → 6,056 行（+23%）、モジュールの増減なし
- 飽和 ply の流れる先に無検査の加算・減算は**残っていない**（rust が全経路を追跡）

## 数えた結果（architecture）

| 知識 | R7 | R8 | **R9** |
| --- | --- | --- | --- |
| 手番記号 | 2 | 2 | **2** |
| 注記の印 | 1 | 1 | **1** |
| 行末の集合 | 2 | 2 | **2** |
| 終局語 | 3 | 3 | **3** |
| 駒種の表記 | 4 | 4 | **4** |
| 「これは `変化：` の行か」 | 1 | 1 | **1** |
| 行頭の余白とは何か | 6 | 4 | **4**（うち `ki2.rs:62` の `space0` が R9-03） |
| **コロンとは何か** | — | 1 と報告 | **3**（`colon` / 直書き / 全角のみの key-value） |

## 見ていない範囲

- `.ki2` / `.csa` / `.jkf` の実コーパスが1件も無い
- CSA 経路はラウンド8の差分に無いため誰も読んでいない
- obs-shogi はソース読みと `node_modules/tsshogi` の実装読みのみ。ビルドしていない
- 盤面図の**行の中**（桁ずれ、駒種の異体）、消費時間の異常値
- 深い入れ子の `forks`（GAP-019）、`normalize` の冪等性、BOM（GAP-006）

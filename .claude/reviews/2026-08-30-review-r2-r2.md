# レビュー review-r2 ラウンド2

- 日付: 2026-08-30
- 範囲: `git diff origin/main...HEAD`（ブランチ `fix/review-r2`）
- 対象コミット: レビュー時 `dd1036c` 前後。修正後 `7c562c6`
- 走らせた reviewer: comment / architecture / perf（ラウンド1で落ちた3本）
- 所見 ID は R4-xx

ラウンド1は `.claude/reviews/2026-08-30-review-r2-r1.md`（R3-xx）。

---

## 直したもの

| # | reviewer | 深刻度 | 所見 | コミット |
| --- | --- | --- | --- | --- |
| R4-01 | architecture | **BLOCK** | **このラウンドの修正が作った退行。** `ORIGIN_UNSTATED` を消す責務がどこにも無く、正規化に失敗した分岐の手が `(00)` として KIF に書かれる。**このクレート自身がそれを読み戻して再び番兵にするので、壊れた棋譜が検出されずに往復する** | `097d6a5` |
| R4-02 | architecture | **BLOCK** | **同上。** `position_known` が本譜しか止めず、分岐の再帰が gate の外。局面を失ったあとの分岐が誤った盤面で正規化され、`correct_color` が先手の手を後手の駒打ちに書き換える | `097d6a5` |
| R4-03 | comment | **BLOCK** | 削除済みの `retain_mut` を説明するコメントが残り、「分岐は正規化を通っている」という偽の前提を述べている | `097d6a5` |
| R4-04 | comment | HIGH | **書き手のコメントが「読み手はこう数える」と断言しているが、読み手はコメント専用ノードを1手として数えていた。** `変化：N手` が1手ずれ、勝者を名指ししない反則勝ちの向きが反転する | `097d6a5` |
| R4-05 | comment | HIGH | R-KI2-002 が言っていないこと（行頭空白）をその ID で書いている。根拠は R-KI2-001 | `097d6a5` |
| R4-06 | perf | HIGH | **`to_ki2` の 96% が `display_single_move_kansuji` 1関数。** 1手あたり 14,256 回の合法性判定で候補集合を作り、結果を文字列に描いてから読み直していた | `f6d63da` |
| R4-07 | perf | MEDIUM | `calculate_from` の `normal_to_candidates` が高いテストを先に走らせる。**さらに書き手と違う候補集合を返す**（ピンされた駒が落ちる） | `f6d63da` |
| R4-08 | perf | MEDIUM | ベンチが読み込みしか測らない。書き出しの 5.2倍の退行が素通しになった。クリーンチェックアウトでも動かない（R2-58） | `7c562c6` |
| R4-09 | comment / architecture | MEDIUM | `try_to_usi_owned` の doc が呼んでいない関数のエラーを返すと書き、`# Errors` が3系統のうち1つしか挙げていない | `097d6a5` |
| R4-10 | comment | MEDIUM | `resumed` は「再開した」ではなく「終局語を通過した」。投了のあとの手でも真になる | `097d6a5` |
| R4-11 | comment | MEDIUM | 変更の経緯が過去形で4箇所（うち2箇所はこのブランチで新規） | `097d6a5` |
| R4-12 | comment | MEDIUM | D5 が「コードは D5 を引け」と名指しした `ki2_phrase` だけ R-KI2-006 のまま | `097d6a5` |
| R4-13 | comment | MEDIUM | issue 番号を伴わない TODO が4件（CSA writer） | `097d6a5` + GAP-014 |
| R4-14 | architecture | MEDIUM | `impl ToUsi` が外部 trait の契約を破っており、`clippy.toml` は自クレート内しか守れない。**consumer が `use shogi_core::ToUsi` を書けば同じ地雷が復活する** | `097d6a5`（`to_usi_owned` を impl で上書き） |
| R4-15 | architecture | MEDIUM | 「1手目の手番」の求め方が4箇所目で分岐し、KI2 書き出しだけ `starting_side` を使っていない | `097d6a5` |

## 直していないもの

| # | reviewer | 深刻度 | 所見 | 送り先 |
| --- | --- | --- | --- | --- |
| R4-16 | architecture | MEDIUM | `try_to_{kif,ki2,csa}_owned` が `std::fmt::Error` のまま。USI だけ `ConvertError`。**consumer が失敗理由を区別できない** | `.claude/plans/v0.4.0-release.plan.md`「タグを打つ前に決めること」。揃えるなら 0.4.0 の前が最後の無償の機会 |
| R4-17 | architecture | MEDIUM | `normalizer` が共有知識の置き場に育ち、`handicap` との循環が2件増えた（`ORIGIN_UNSTATED` / `csa.rs` からの参照） | `HIRATE_BOARD` の移動と `notation.rs` の新設は R2-16 / R2-17 と同じ受け皿。別ブランチ |
| R4-18 | comment | MEDIUM | `side_to_move`（初手の手番）と `PartialPosition::side_to_move()`（現在の手番）が同名で同じ式に並ぶ | 改名は差分が広いので別ブランチ |
| R4-19 | comment | MEDIUM | 本文に説明コメントが何段も要る関数が3つ（`write_line` / `normalize_moves` / `move_run`）。分割の合図 | 同上 |
| R4-20 | architecture | — | `ORIGIN_UNSTATED` を `csa.rs` が「CSA の `00`」の判定に使っており、定数名と意味が食い違う | R4-17 と同じ受け皿。**`00` は移動元を述べている**（駒台から）ので、名前を分けるのが正しい |

## 数字

**計測は arm64 ネイティブ。** このマシンの `rustc` は host が x86_64 なので、
素の `--release` は Rosetta で走り数字が約2倍になる。
**前ラウンドの R2-57「44.4 ms」はその artifact だった**（ネイティブでは 21.3 ms）。
`research/repro/README.md` に手順を残した。

コーパス609件、arm64 ネイティブ:

| | ラウンド2の前 | 後 |
| --- | --- | --- |
| `to_ki2` | 225.2 ms | **25.5 ms** |
| `bug_mega` 1件の `to_ki2` | 21.3 ms | **4.6 ms** |
| `parse_ki2_str` | 237.3 ms | **136.8 ms** |
| `to_kif` | 14.2 ms | 13.5 ms |
| `to_csa` | 5.6 ms | 4.6 ms |

**書き出した KI2 は 609件すべてで指し手・分岐・終局・盤面が1バイトも変わらない**
（`research/repro/dump_ki2.rs` で前後をダンプして `diff`。ヘッダは `HashMap` なので順序のみ揺れる）。

往復の忠実度は変わらず: 指し手 86946 → 86946、分岐 1736 → 1736、終局 583 → 583、
読み戻し不能 0。テスト 81。

## 見ていない範囲（reviewer の申告を統合）

- CSA / USI / JKF を入口とする経路の実測（`to_csa` の総時間以外）
- `data/tests/` の期待 JSON が仕様由来か上流実装の出力か
- メモリ使用量・アロケーション回数（時間だけ測った）
- Windows / Linux でのスタックサイズと `rustc` の host triple
- perf の計測中に別の作業が `src/` を書き換えていた。ホットパスには触れていないので数字は有効だが、
  ブランチ側のバイナリが HEAD ちょうどかは確定していない
- comment は `cargo test` を走らせず、ソースと `research/` の読み合わせのみ

## lint / hook / 型で強制できるもの

- **`clippy.toml` に3本入った**: `ToUsi::to_usi_owned` / `display_single_move_kansuji` /
  `LegalityChecker::normal_to_candidates`。後ろ2つはこのラウンドで追加
- **削除済み識別子を指すコメント**は grep で機械化できる。R4-03 はこれ1本で出る
- **要件 ID の実在チェック**も grep で可能（内容の一致は人が見るしかない）
- **`let _ = <Result>`** を `clippy::let_underscore_must_use` で。R4-02 の「エラーを捨てたまま局面を確定させる」形
- **`TODO(#N)` の強制**を `.claude/hooks/verify-gate.sh` へ。現在の未番号 TODO は0件になった
- **ベンチの閾値**が書き出し側の退行を止める唯一の機械的な歯止め。`.claude/verify.sh` は fmt/clippy/test だけ

## 次ラウンドの対象

このラウンドで `normalizer.rs` の曖昧性解消を書き直したので、**7観点すべてを回し直す。**
